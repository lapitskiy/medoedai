# docs/xgb

Документация по XGB пайплайну и подбору параметров:

- `metrics_and_signals.md` — как читать метрики (`y_non_hold`, `pred_non_hold`, `f1(1)`, `prec(1)`, `recall(1)`).
- `goal_prompt_5pct_month.md` — “prompt/памятка” под цель 5–6%/мес при 5–10 сделках (и env для отбора `grid_final`).

